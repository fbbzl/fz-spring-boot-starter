# application.yml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://123.60.157.107/lj_cloud?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: TUju=LUk^&UkvWYp

    # HikariCP 连接池配置（可选但推荐）
    hikari:
      # 连接池名称
      pool-name: lj_dev
      # 最大连接数
      maximum-pool-size: 20
      # 最小空闲连接数
      minimum-idle: 5
      # 连接超时时间（毫秒）
      connection-timeout: 30000
      # 空闲连接超时时间（毫秒）
      idle-timeout: 600000
      # 连接最大存活时间（毫秒）
      max-lifetime: 1800000
      # 验证超时时间（毫秒）
      validation-timeout: 5000
      # 连接测试查询语句
      connection-test-query: SELECT 1